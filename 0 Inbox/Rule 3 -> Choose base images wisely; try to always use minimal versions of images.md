We have `nginx:1.28`. It is Debian based. But we also have `nginx:1.28-alpine` - it is much smaller version, yet functional.

We have `alpine:3.20` to run containers on linux. It might have some problems like hard debugging, some packages unavailability. However, we get smallest base image with package manager in return.

Links:

202609041600

