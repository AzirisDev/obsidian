 It is a platform that makes it simple to run those Linux commands and manage them.
- `Docker CLI` - command line tool
- `Docker Daemon` - background service managing containers
- `Docker Desktop` - GUI + Linux VM
For Linux machine we only need `Docker Deamon and CLI` to run them natively.

Alternatives to Docker: Podman, Rancher Desktop.

Links:

202608312237

