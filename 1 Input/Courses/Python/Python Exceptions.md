 ```
try:
	x = int(input('x?'))
	print(x)
except ValueError:
	print('x is not an integer')
 ```

`SyntaxError` - syntax mistakes
`ValueError` - not right type
`NameError` - variable not defined

`break` - breaks the loop
`pass` - pass to the next iteration in loop

Links:

202510291127

