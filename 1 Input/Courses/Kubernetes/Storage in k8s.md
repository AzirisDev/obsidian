Pods/containers are ephemeral, they might die. That is why we need some place to store information.

`Volume` is a directory that's accessible to containers in a pod, solving two problems: data loss when a container restarts, and sharing files between containers in the same pod.

There are many types of volumes:
- [[Ephemeral storage]]
- [[Persistent storage]]
  

Links:

202609151951

