#### [[What is networking on containers level?]]

Docker handles the networking when we do `docker run` and sets up all necessary `network namespaces` to the containers. It creates `veth pairs` and `bridge` to make connection between containers and to the internet.

#### [[Networking lab00 - docker demo code & architecture]]
#### [[Networking lab00 - build container network from scratch]]
#### [[Networking lab00 - enable internet access]]

#### [[Networking lab00 - docker compose demo]]




Links:

202609221226

